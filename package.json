{
  "name": "sci-info-system",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "./public/main.js",
  "homepage": "./",
  "private": false,
  "devDependencies": {
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0",
    "antd": "^3.26.6",
    "react-app-rewired": "^2.1.5",
    "customize-cra": "^0.9.1",
    "babel-plugin-import": "^1.13.0"
  },

  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "electron": "electron .",
    "pack": "electron-packager ./ 科研成果统计系统 --platform=win32 --arch=x64 --icon=./build/favicon.ico --out=./dist/ --asar --app-version=1.0.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
