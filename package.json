{
  "name": "sci-info-search",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "./public/main.js",
  "homepage": "./",
  "private": true,
  "dependencies": {
    "axios": "^0.19.1",
    "electron": "^7.1.8",
    "html2canvas": "^1.0.0-rc.5",
    "nightmare": "^3.0.2",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build",
    "start-electron": "electron .",
    "pack": "electron-packager ./ 科研成果统计系统 --platform=win32 --arch=x64 --icon=./sci-info-search/file/icon.ico --out=./ --asar --app-version=1.0.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
